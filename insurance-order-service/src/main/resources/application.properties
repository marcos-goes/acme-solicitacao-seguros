spring.application.name=insurance-order-service

# Database PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/parking_slot_draw
spring.jpa.properties.hibernate.default_schema=parkingslot
spring.datasource.username=dbo
spring.datasource.password=abc@123
spring.datasource.driver-class-name=org.postgresql.Driver

# Liquibase
spring.liquibase.change-log=classpath:/liquibase/master.xml
spring.liquibase.enabled=true
#spring.liquibase.user=${LIQUIBASE_USERNAME}
#spring.liquibase.password=${LIQUIBASE_PASSWORD}
spring.liquibase.liquibase-schema=public
